services:
  ollama:
    image: ollama/ollama:latest
    ports:
      - "11434:11434"
    volumes:
      - ollama:/root/.ollama
    restart: unless-stopped

  # 1회 모델 다운로드(기본 llama3.1:8b). OLLAMA_MODEL로 변경 가능.
  ollama-pull:
    image: ollama/ollama:latest
    depends_on:
      - ollama
    environment:
      - OLLAMA_HOST=http://ollama:11434
    entrypoint: ["/bin/sh", "-c", "ollama pull ${OLLAMA_MODEL:-llama3.1:8b}"]
    restart: "no"

  app:
    # 사전빌드 이미지(GHCR): `docker compose pull`로 빌드 없이 사용.
    # 직접 빌드하려면 `docker compose up --build` (아래 build 사용).
    image: ghcr.io/spark798/iamspeaker:latest
    build: .
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - OLLAMA_HOST=http://ollama:11434
      - OLLAMA_MODEL=${OLLAMA_MODEL:-llama3.1:8b}
      - DATA_DIR=/app/data
      - DATABASE_URL=file:/app/data/iamspeaker.db
    volumes:
      # 업로드/녹음/SQLite/모델 영속 (프라이버시: 데이터는 로컬에만)
      - ./data:/app/data
    depends_on:
      - ollama
    restart: unless-stopped

volumes:
  ollama:
